apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-webserver.fullname" . }}
  labels:
    {{- include "go-webserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "go-webserver.selectorLabels" . | nindent 4 }}
